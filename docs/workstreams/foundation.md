# Foundation workstream

## Status

Not started.

## Ownership

Root configuration, Docker Compose, database migrations, shared schemas, generated clients, CI, and base developer commands.

## Next milestone

Create the Phase 0 vertical foundation so one command starts the minimal web/API/database stack and baseline checks pass. Once this is committed to `develop`, parallel worktrees may be created.
